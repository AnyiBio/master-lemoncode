import React from "react";
import { AppLayout } from "@/layouts";
import { TableContainer } from "@/pods/table";

export const OrganizationPage: React.FC = () => {
  return (
    <AppLayout>
      <TableContainer />
    </AppLayout>
  );
};